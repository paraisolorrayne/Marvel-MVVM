import Foundation

struct ComicDTO {
    struct Resume {
        let image: String
        let title: String
    }

    struct Detail {
        let image: String
        let title: String
        let price: String
        let digitalPrice: String
    }
}
